<resources>
    <!-- Base application theme. -->
    <style name="ToolBarStyle" parent="ThemeOverlay.AppCompat.ActionBar">
        <!-- Parent theme sets colorControlNormal to textColorPrimary. -->
        <item name="android:textColorPrimary">@android:color/white</item>
    </style>
    <style name="CaptionText">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">@dimen/margin10dp</item>
        <item name="android:textColor">@android:color/darker_gray</item>
        <item name="android:textSize">@dimen/caption_text_size</item>
    </style>
    <style name="RequestText">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">@dimen/margin10dp</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/request_text_size</item>
    </style>
    <style name="TableRowStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/divider_height</item>
        <item name="android:layout_marginTop">@dimen/margin10dp</item>
        <item name="android:layout_marginLeft">@dimen/margin10dp</item>
        <item name="android:layout_marginRight">@dimen/margin10dp</item>
        <item name="android:background">@color/rowColor</item>
    </style>
    <style name="TextStatusRequestStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
</resources>
